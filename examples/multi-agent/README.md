# Multi-Agent (Archived)

> **Note:** Agent orchestration and the A2A protocol were removed in the agent-removal-sdd-pivot.
> The YAML files in this directory reference `agents:` and `a2a:` config fields that are no longer
> supported and will be silently ignored by gridctl.
>
> These files are kept for historical reference. For current examples, see:
> - [`getting-started/mcp-basic.yaml`](../getting-started/mcp-basic.yaml) — multi-server stacks
> - [`access-control/tool-filtering.yaml`](../access-control/tool-filtering.yaml) — tool access control
> - [`registry/`](../registry/) — skills registry and executable workflows
