name: chained-calculation
description: Cross-server chain — add numbers, echo the result, then get the time
input:
  - name: a
    description: First number
    required: true
  - name: b
    description: Second number
    required: true
steps:
  - tool: math-server__add
    arguments:
      a: "{{input.a}}"
      b: "{{input.b}}"
  - tool: text-server__echo
    arguments:
      message: "{{step1.result}}"
  - tool: math-server__get_time
    arguments: {}
timeout: "15s"
tags:
  - cross-server
  - chaining
state: draft
